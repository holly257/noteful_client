import React from 'react';

function Header(){
    return(
        <header>
            <h1>Noteful</h1>
            {/* click to nav to main page */}
        </header>
    )
}

export default Header;